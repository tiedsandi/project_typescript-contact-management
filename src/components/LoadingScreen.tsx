const LoadingScreen = () => (
  <div className="h-[60vh] w-full flex justify-center items-center">
    <div className="inline-block w-12 h-12 border-4 border-gray-300 border-t-gray-600 rounded-full animate-spin"></div>
  </div>
);

export default LoadingScreen;
